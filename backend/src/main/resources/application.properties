spring.datasource.url=/h/jdbc:pos/h/tgresql://aws-1-eu-north-1.pooler.sup/h/abase.com:5432/postgres?sslmode=require/h/
spring.datasource.username=postgres.wavftsccaree/h/dxrapqfb
spring.datasource.password=/f/nq4OHp/f/CmxHzKog2m//
spring.datasource.hikari.maximum-pool-size=${DB_POOL_SIZE:5}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

jwt.secret=${JWT_SECRET:change-this-development-secret-to-a-long-random-value}
jwt.expiration=${JWT_EXPIRATION:86400000}

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
server.port=${PORT:8080}
## Default CORS allowed origins. Override with CORS_ALLOWED_ORIGINS env var.
## When not provided, a wildcard pattern is used so deployed frontends can authenticate.
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:*}
